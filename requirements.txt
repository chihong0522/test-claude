# Core (already used by the bot)
httpx>=0.28
websockets>=13.0

# Live trading (required for --mode live / dry-run)
py-clob-client>=0.1.0
eth-account>=0.13.0
python-dotenv>=1.0.0

# Signal accuracy analysis (used by refresh_smart_wallets.py)
scipy>=1.12.0
